{
  "name": "fictyfact-crazygames-html",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "start": "vite",
    "dev": "vite",
    "build": "vite build",
    "build:pages": "vite build --mode pages",
    "preview": "vite build && vite preview",
    "preview:pages": "npm run build:pages && vite preview --open /ProfessorPlease/"
  },
  "dependencies": {
    "papaparse": "^5.5.3",
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "vite": "^7.1.10"
  }
}
